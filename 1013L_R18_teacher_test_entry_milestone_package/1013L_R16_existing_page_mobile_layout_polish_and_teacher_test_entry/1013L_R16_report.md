# 1013L R16 Existing Page Mobile Layout Polish And Teacher Test Entry

R16 keeps the existing R13 page and adds a mobile-only CSS patch for the big-unit route. On narrow screens, the legacy notebook directory and right reference rail are hidden for the big-unit route so the big-unit body becomes visible.

It also writes a local teacher test entry with the key static routes.

Boundary remains static-only: no runtime fetch, no provider/model, no database/memory/Feishu write, no formal apply, and no main project push.
